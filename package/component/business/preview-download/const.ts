const TYPE = {
  PIC: 'PIC',
  AUDIO: 'AUDIO',
  VIDEO: 'VIDEO',
  OFFICE: 'OFFICE',
  PDF: 'PDF'
};

const PIC = ['.png', '.jpg', '.jpeg'];

const AUDIO = ['.audio', '.mp3', '.ogg'];

const VIDEO = ['.video', '.mp4'];

const OFFICE = ['.docx', '.xlsx', '.ppt'];

const PDF = ['.pdf'];

export { TYPE, PIC, AUDIO, VIDEO, OFFICE, PDF };
