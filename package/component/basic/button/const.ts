// 按钮类型
const SIZE = {
  small: 'small', // 小尺寸
  middle: 'middle', // 中尺寸
  large: 'large' // 大尺寸
};

// 按钮类型
const MODE = {
  normal: 'normal', // 白色背景，黑色文字，主要用于取消等无操作按钮
  fail: 'fail', // 红色背景，主要用于拒绝
  success: 'success' // 蓝色背景，主要用于通过或者是主按钮
};

export { SIZE, MODE };
